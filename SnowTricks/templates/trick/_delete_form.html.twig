<form method="post" action="{{ path('trick_delete', {'slug': trick.slug}) }}" onsubmit="return confirm('Vous êtes sur de vouloir supprimer ce trick ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ trick.slug) }}">
    <button class="btn btn-danger" style="margin-bottom: 2%" type="submit">Supprimer le trick</button>
</form>
