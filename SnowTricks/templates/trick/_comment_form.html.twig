{{ form_start(form_comment) }}


<div class="my-custom-class-for-errors">
    {{ form_errors(form_comment) }}
</div>

<div class="row">
    <div class="col">
        {{ form_row(form_comment.content, { 'label': 'Laissez un commentaire :', 'attr': {'class': 'form-control'} }) }}
    </div>

</div>
<div class="d-flex flex-row-reverse" style="margin: 2%">
    <button class="btn btn-primary" style="font-size: 14px; padding: 3px">{{ button_label|default('Publier le commentaire') }}</button>

</div>
{{ form_end(form_comment) }}


